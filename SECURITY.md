# سیاست امنیتی

## گزارش آسیب‌پذیری

لطفاً جزئیات آسیب‌پذیری یا کلید افشاشده را در Issue عمومی قرار ندهید. از بخش
Security Advisories مخزن و گزینه **Report a vulnerability** استفاده کنید.

## اسرار و فایل‌های محلی

موارد زیر هرگز نباید Commit شوند:

- `.env` و هر فایل دارای API key یا token
- فایل‌های `*.session` و `SESSION_STRING`
- دیتابیس‌های `*.sqlite*`
- cookieهای مرورگر یا فایل cookie مربوط به yt-dlp
- خروجی دانلود، لاگ و فایل موقت

نسخه جدید این موارد را از متغیر محیطی می‌خواند و `.gitignore` نیز الگوهای رایج را
مسدود می‌کند. با این حال، `.gitignore` جای اسکن قبل از انتشار را نمی‌گیرد.

## چرخش credentialها

این مخزن با تاریخچه‌ای مستقل و بدون انتقال فایل‌ها یا Commitهای پروژهٔ قدیمی منتشر
شده است. با این حال، حذف سابقه باعث بی‌اعتبارشدن credentialهایی که قبلاً استفاده
شده‌اند نمی‌شود. هر Telegram session، Bot token یا API key قدیمی را revoke/rotate
کنید و فقط مقدارهای تازه را از طریق `.env` یا secret manager در محیط اجرا قرار دهید.

اگر secret تازه‌ای سهواً Commit شد، فوراً آن را باطل کنید؛ سپس پیش از ادامهٔ انتشار،
تمام referenceهای Git (از جمله branch، tag و pull request) را پاک‌سازی کنید.
