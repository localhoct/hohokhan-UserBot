HELP_TEXT = """
<b>راهنمای HoHoKhan</b>

<b>رسانه</b>
• <code>.music نام آهنگ</code> — دانلود صوت از YouTube
• <code>.yt عبارت</code> — جست‌وجوی YouTube
• ارسال لینک YouTube، Instagram، TikTok، X/Twitter یا Likee — دانلود ویدیو

<b>ابزارها</b>
• <code>.wiki عبارت</code> — جست‌وجوی ویکی‌پدیای فارسی
• <code>.weather شهر</code> یا <code>آب و هوای شهر</code>
• <code>.qr متن</code> — ساخت QR؛ برای خواندن QR روی عکس ریپلای کنید
• <code>ocr</code> روی عکس — استخراج متن
• <code>.photo</code> روی استیکر و <code>.sticker</code> روی عکس
• <code>.hash sha256 متن</code>، <code>.pass 20</code>، <code>.rot13 متن</code>
• <code>.ips 192.168.1.0/24</code> — اطلاعات شبکه
• ارسال فایل <code>.ovpn</code> — تبدیل hostname در remote به IP
• <code>.info</code>، <code>ping</code> و <code>sysinfo</code>

<b>پاسخ خودکار</b> (مالک)
• <code>addans محرک | پاسخ</code>
• <code>delans محرک</code>، <code>anslist</code>، <code>cleanans</code>

<b>مدیریت</b> (مالک/کاربر مجاز)
• <code>del</code>، <code>.kick</code>، <code>.mute</code>، <code>.unmute</code>
• <code>.add</code>، <code>.join لینک</code>، <code>.leave</code>
• <code>tag</code>، <code>قفل گروه</code>، <code>باز کردن گروه</code>
• <code>block</code> و <code>unblock</code>
""".strip()

ANSWERS = (
    "جانم؟ 👀",
    "هوهوخان آماده‌ست 😄",
    "بفرمایید!",
)
